[defaults]
inventory = inventory.yaml
remote_user = ubuntu
private_key_file = ./gcp-key.pem
host_key_checking = False
retry_files_enabled = False
log_path = ./ansible.log
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
become_user = root
